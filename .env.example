# 数据模式: jnb(走 Tushare 真实行情) 或 websearch(纯角色对话，不走数据接口)
DATA_MODE=jnb

# Tushare API 配置（仅 DATA_MODE=jnb 时必填）
# Token 获取: https://tushare.pro/user/token
TUSHARE_TOKEN=你的_tushare_token
# Tushare 中转 API 地址，格式: https://your-proxy-server.com
TUSHARE_API_URL=
# 可选，实时行情验证地址，一般不需要
# TUSHARE_VERIFY_TOKEN_URL=

# 数据库配置
DATA_DIR=data
DB_PATH=data/stock_data.db
